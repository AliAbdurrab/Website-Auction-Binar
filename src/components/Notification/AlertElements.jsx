import styled from 'styled-components';
import { Alert } from 'react-bootstrap';

export const AlertModifier = styled(Alert)`
    box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.15);
    border-radius: 12px;
`